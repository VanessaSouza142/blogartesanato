<div class="row">
  <div class="col-md-2">
    <section class="artisan_info center">
      <%= gravatar_for(comment.artisan, size: 60) %>
    </section>
  </div>
  <div class="col-md-8 well">
    <p><%= comment.description %></p>
    <p>
      <span class="quiet"><small>Criado: <%= time_ago_in_words(comment.created_at) %> atrás por <%= comment.artisan.name %></small></span>
    </p>
  </div>
</div>